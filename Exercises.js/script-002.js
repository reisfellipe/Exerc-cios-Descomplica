var nome = "Fellipe Reis";
console.log(typeof(nome));
if(typeof(nome) == "string") {
    console.log(nome.toLocaleUpperCase());
} //here happened a comparison if the typeof is the string, then transform the contain uppercase